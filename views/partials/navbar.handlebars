<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="pull-left">
      {{#if currentUser}}
        <p>Hi, {{currentUser.username}}</p>
        <a href="/reviews/new" class="btn btn-primary">New Review</a>
      {{/if}}
    </div>

    <ul class="nav navbar-nav navbar-right">
      {{#if currentUser}}
        <li><a href="/logout" class="btn btn-danger">Logout</a></li>
      {{else}}
        <li><a href="/login" class="btn btn-success">Login</a></li>
        <li><a href="/sign-up" class="btn btn-info">Sign Up</a></li>
      {{/if}}
    </ul>
  </div>
</nav>